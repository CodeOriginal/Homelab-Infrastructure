# ===== Global Defaults (Example) =====
# Copy this file to a real .env (not committed) and adjust as needed.

# Timezone for containers (ensure it matches your locale)
TZ=America/Denver

# Default UID/GID for container processes writing to /srv mounts
PUID=1000
PGID=1000

# Common domain root (optional; used to construct service hostnames)
BASE_DOMAIN=example.com

# If you want to set default router entrypoint names or cert resolver (optional)
TRAEFIK_ENTRYPOINT_WEB=web
TRAEFIK_ENTRYPOINT_WEBSECURE=websecure
TRAEFIK_CERT_RESOLVER=letsencrypt
